<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://kit.fontawesome.com/c976e2c281.js" crossorigin="anonymous"></script>
    <!-- Latest compiled and minified CSS -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.2/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="./css/main.css">
    <title>Document</title>
</head>

<body>
    <div class="container-fluid bg-image header">
        <div class="row">
            <div class="col-9 mx-auto">
                {{>navbar}}
                <div class=" text-center text-warning p-3">
                    <h1 class="font-weight-bold ">Welcome to MERN BackEnd Course</h1>
                    <p>The Best Course On Youtube</p>
                </div>
            </div>
        </div>
    </div>

    <div class="container">

        <div class="row">
            <div class="col-md-5 col-8 mx-auto form-div ">

                <div class="col-11 mx-auto">

                    <form action="./url" method="POST">
                        <h2 class="text-center p-2">Make Short Id</h2>
                        {{#if result}}
                        <p class="text-center ">URL Generated : http://localhost:5000/url/{{result.shortId}}</p>
                        {{/if}}
                        <div class="form-group row">
                            <input type="url" class="form-control" placeholder="Enter Your Original Url"
                                name="redirectUrl" required>
                        </div>
                        <div class="form-group row">

                            <button type="submit" class="btn btn-primary btn-lg mx-auto">Generate</button>

                        </div>
                    </form>
                </div>


            </div>
        </div>

        <div class="row">
            <div class="col-md-8 col-12 mx-auto">
                {{#if allurls.length}}
                <table class="table table-dark">
                    <thead>
                        <tr>
                            <th scope="col">#</th>
                            <th scope="col">Redirect</th>
                            <th scope="col">Short Id</th>
                            <th scope="col">Clicks</th>
                        </tr>
                    </thead>
                    <tbody>
                        {{#each allurls}}
                        <tr>
                            <th scope="row">{{@index}}</th>
                            <td>{{this.redirectUrl}}</td>
                            <td>{{this.shortId}}</td>
                            <td>{{this.visitHistory.length}}</td>
                        </tr>
                        {{/each}}
                    </tbody>
                </table>
                {{/if}}

            </div>
        </div>

    </div>
    {{>footer}}



    <!-- jQuery library -->
    <script src="https://cdn.jsdelivr.net/npm/jquery@3.6.4/dist/jquery.slim.min.js"></script>

    <!-- Popper JS -->
    <script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.1/dist/umd/popper.min.js"></script>

    <!-- Latest compiled JavaScript -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.6.2/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>